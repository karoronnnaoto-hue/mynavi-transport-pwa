# マイナビ交通費サーチ PWA

GitHub Actions上でマイナビ2028の公開検索一覧を差分巡回し、交通費・宿泊費・開催日・開催地をiPhoneから検索できるPWAです。

## 巡回の役割分担

| ジョブ | 頻度 | 処理 |
|---|---:|---|
| Discover new internships | 30分ごと | 検索一覧を巡回し、新規の企業ID＋コースIDを発見。その実行内で新規募集の詳細を取得 |
| Refresh existing internships | 6時間ごと | 既存募集を古い確認順に分散再確認し、交通費・日程・締切などの変更を更新 |
| Check urgent deadlines | 1時間ごと | 締切が3日以内の掲載中募集だけを再確認 |
| Clean up closed internships | 1日1回（日本時間3時台） | 終了・満席・中止候補を再確認し、状態を更新。履歴は削除しない |

GitHub ActionsのcronはUTCです。実行は混雑により多少遅れることがあります。

初回に母集団を作る場合は、`Discover new internships` を手動実行し、`full_scan` を `true` にします。全件収集時はブラウザで実際に「交通費支給あり」条件と「次の30社」をクリックして一覧リンクを集めるため、フォームPOSTをrequestsで再現して「不正な操作」判定に落ちる経路を避けます。初回はタイムアウトを避けるため、一覧カタログ作成、詳細DB化、表示用ビュー生成を分担します。カタログは一覧収集直後に先にコミットし、詳細取得は500件ずつ `detail_batch` を自動再起動して継続します。詳細取得した情報は `data/items.json` に全件DBとして蓄積し、`docs/data/jobs.json` はそのDBから「交通費あり・関東除外・理系限定除外」をかけた表示用ビューとして生成します。関東と非関東が混在する募集は、表示用ビューで関東以外の開催地だけを残します。

## マイナビHTMLの前提

- 詳細ページの会社名は `h1`、コース名は `#courseName` にあります。
- 交通費・宿泊費・開催地域・応募締切日は、本文の文字検索ではなく `table.dataTable02` の `td.heading` 行から取得します。
- 一覧ページのコースリンクは `displayInternship/index?corpId=...&optNo=...` です。リンク文字列はコース名ヒントとして保存します。
- 一覧ページの「次の30社」は通常の `href` ではなく、`searchCorpListByIsForm` をJavaScriptでPOSTする導線です。`full_scan=true` では `scripts/discover_browser.js` がブラウザ操作としてクリックし、通常巡回だけ既存の軽量requests巡回を使います。

## 重複排除

1. 企業ID＋コースID
2. 追跡パラメータを除いた正規化URL
3. 企業名＋コース名＋開催日＋開催地の内容指紋

同一コースが複数の検索カテゴリや一覧ページに出ても1件に統合します。

## 主な機能

- 交通費の最低金額、全額・実費、宿泊費で絞り込み
- 交通費欄があり、支給なしではない募集を母集団化
- 解析済み詳細は全件DB化し、表示・分析ビューで関東を切り落とす
- 参加条件が理系限定の募集を除外
- マイナビ詳細ページの業種カテゴリで絞り込み
- 開催地を地方・都道府県で絞り込み
- 交通費あり件数、金額判定済み件数を表示
- 開催日を単日または期間で指定
- 終了・満席は初期状態で非表示（必要時に表示可能）
- お気に入りをiPhone内に保存
- ホーム画面へ追加してアプリとして利用

## 公開方法

1. フォルダの中身をGitHubリポジトリへアップロード
2. Settings → Pages → Source を `GitHub Actions` に設定
3. Actionsの `Discover new internships` を手動実行
4. Pages URLをiPhoneのSafariで開き、「ホーム画面に追加」

`targets.txt`へ個別コースURLを追加すると、全体巡回とは別に優先登録できます。
